<Controls:MetroWindow x:Class="SimpleRenamer.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        Title="Simple Renamer - Settings" Height="345" Width="675" MinHeight="345" MinWidth="675" MaxHeight="345" MaxWidth="675" Icon="tv.ico">
    <Controls:MetroWindow.RightWindowCommands>
        <Controls:WindowCommands>
            <Button Name="SaveButton" Click="SaveButton_Click">
                <StackPanel Orientation="Horizontal">
                    <Rectangle Width="20" Height="20" Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}">
                        <Rectangle.OpacityMask>
                            <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_save}" />
                        </Rectangle.OpacityMask>
                    </Rectangle>
                </StackPanel>
            </Button>
        </Controls:WindowCommands>
    </Controls:MetroWindow.RightWindowCommands>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="325" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Vertical" >
            <Controls:ToggleSwitch Name="SubdirToggle" Margin="5,0" OnLabel="Scan Subdirectory" OffLabel="Scan Root Only" IsChecked="{Binding Path=SubDirectories}" />
            <StackPanel Orientation="Horizontal" Height="30">
                <Label>Watch Folder(s)</Label>
                <Button Style="{DynamicResource SquareButtonStyle}" Name="AddFolderButton" Content="Add" HorizontalAlignment="Left" Margin="5,5,5,0" Height="20" VerticalAlignment="Top" Width="50" Click="AddFolderButton_Click"/>
                <Button Style="{DynamicResource SquareButtonStyle}" Name="DeleteFolderButton" Content="Del" HorizontalAlignment="Left" Margin="5,5,5,0" Height="20" VerticalAlignment="Top" Width="50" Click="DeleteFolderButton_Click"/>
            </StackPanel>
            <ListBox Height="100" Name="WatchListBox" Margin="5,0,5,5" HorizontalContentAlignment="Stretch">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding}" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <StackPanel Orientation="Horizontal" Height="30">
                <Label Margin="5,0,5,0">File Extension</Label>
                <Button Style="{DynamicResource SquareButtonStyle}" Name="AddExtensionButton" Content="Add" HorizontalAlignment="Left" Margin="5,5,5,0" Height="20" VerticalAlignment="Top" Width="50" Click="AddExtensionButton_Click"/>
                <Button Style="{DynamicResource SquareButtonStyle}" Name="DeleteExtensionButton" Content="Del" HorizontalAlignment="Left" Margin="5,5,5,0" Height="20" VerticalAlignment="Top" Width="50" Click="DeleteExtensionButton_Click"/>
            </StackPanel>
            <ListBox Height="100" Name="ExtensionsListBox" Margin="5,5,5,5" HorizontalContentAlignment="Stretch">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding}" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Vertical" >
            <Controls:ToggleSwitch Name="RenameToggle" Margin="0" OnLabel="Rename Files" OffLabel="Keep Original" IsChecked="{Binding Path=RenameFiles}" />
            <Controls:ToggleSwitch Name="CopyToggle" Margin="0" OnLabel="Copy Files" OffLabel="Move Files" IsChecked="{Binding Path=CopyFiles}" />
            <Label>File Name Format</Label>
            <TextBox Name="FileFormatTextBox" Margin="5" Text="{Binding Path=NewFileNameFormat, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <Label>Destination Root Folder (TV)</Label>
            <StackPanel Orientation="Horizontal">
                <TextBox Name="DestinationTextBox" Margin="5" Text="{Binding Path=DestinationFolderTV, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="280"/>
                <Button Style="{DynamicResource SquareButtonStyle}" Name="BrowseDestinationButton" Click="BrowseDestinationButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <Rectangle Width="20" Height="20" Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}">
                            <Rectangle.OpacityMask>
                                <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_folder_ellipsis}" />
                            </Rectangle.OpacityMask>
                        </Rectangle>
                    </StackPanel>
                </Button>
            </StackPanel>
            <Label>Destination Root Folder (Movie)</Label>
            <StackPanel Orientation="Horizontal">
                <TextBox Name="DestinationMovieTextBox" Margin="5" Text="{Binding Path=DestinationFolderMovie, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="280"/>
                <Button Style="{DynamicResource SquareButtonStyle}" Name="BrowseDestinationMovieButton" Click="BrowseDestinationMovieButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <Rectangle Width="20" Height="20" Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}">
                            <Rectangle.OpacityMask>
                                <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_folder_ellipsis}" />
                            </Rectangle.OpacityMask>
                        </Rectangle>
                    </StackPanel>
                </Button>
            </StackPanel>
            <Button Style="{DynamicResource SquareButtonStyle}" Name="RegexExpressionButton" Content="Regex Expressions" HorizontalAlignment="Left" Margin="0,5,0,5" VerticalAlignment="Top" Width="150" Click="RegexExpressionButton_Click"/>
        </StackPanel>
    </Grid>
</Controls:MetroWindow>
